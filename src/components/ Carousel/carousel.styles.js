import { makeStyles } from "@mui/styles";

const useStyles = makeStyles({
  post: {
    width: "100%",
    maxHeight: "700px",
  },
  carouselImage: {
    objectFit: "contain",
    aspectRatio: "1/1",
  },
});

export default useStyles;
